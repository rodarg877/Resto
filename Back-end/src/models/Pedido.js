export default class Pedido {
    constructor(platos, user, estado, direccion, id) {
        this.platos = platos;
        this.user = user;
        this.estado = estado;
        this.direccion = direccion;
    }
}
