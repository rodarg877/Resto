export default class Plato {
    constructor(nombre, descripcion, img, precio) {
        this.nombre = nombre;
        this.descripcion = descripcion;
        this.img = img;
        this.precio = precio;
    }
}
